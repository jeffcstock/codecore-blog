<% if user_signed_in? %>
  <h2>Create a Comment</h2>
  <%= form_for [@post, @comment], remote: true do |f| %>
    <div>
        <%= f.label :body %><br />
        <%= f.text_area :body %>
    </div>
    <%= f.submit %>
  <% end %>
<% end %>
